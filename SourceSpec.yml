version: 0.2
phases:
  
  build:
    commands:
      - echo Build started on `date`
      - aws s3 cp --recursive . s3://${REPO_BUCKET}/${REPO_PATH}
 
  post_build:
    on-failure: CONTINUE

artifacts:
  files:
    - '**/*'
  base-directory: .
