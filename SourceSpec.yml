version: 0.2
phases:
  
  build:
    commands:
      - echo Build started on `date`
      - zip -r ${REPO_PATH}.zip ../src
      - aws s3 cp ${REPO_PATH}.zip s3://${REPO_BUCKET}/
 
  post_build:
    on-failure: CONTINUE

artifacts:
  files:
    - '**/*'
  base-directory: .
