version: 0.2
phases:
  
  build:
    commands:
      - echo Build started on `date`

      - pwd
      - ls -a

      - aws s3 cp --recursive . s3://${DEPLOY_BUCKET}/
 
  post_build:
    on-failure: CONTINUE

artifacts:
  files:
    - '**/*'
  base-directory: .
