version: 0.2
phases:
  
  build:
    commands:
      - echo Build started on `date`
      - aws s3 cp . s3://${REPO_BUCKET}/${REPO_NAME}
 
  post_build:
    on-failure: CONTINUE

artifacts:
  files:
    - '**/*'
  base-directory: .
